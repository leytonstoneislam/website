<%= form_for(@homepageentry) do |f| %>
  <% if @homepageentry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@homepageentry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @homepageentry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :title %><br />
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :content %><br />
      <%= f.text_area :content %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
